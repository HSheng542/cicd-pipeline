title = "DevSecOps demo finding configuration"

[extend]
useDefault = true

[[rules]]
  id = "demo-fake-api-key"
  description = "Detect the harmless fake API key used in the training demo"
  regex = '''DEMO_FAKE_API_KEY_[A-Za-z0-9]{24}'''
  secretGroup = 0
